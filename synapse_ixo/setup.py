from setuptools import setup, find_packages

setup(
    name="ixo",
    version="0.0.1",
    packages=find_packages(),
    description="Ixo Synapse Agent",
    include_package_data=True,
    zip_safe=True,
    install_requires=[],
)
